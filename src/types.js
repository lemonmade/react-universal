// @flow

export type ConfigType = {
  projectRoot: string,
  appDir: string,
  buildDir: string,
  dataDir: string,
  componentDir: string,
  sectionDir: string,
  publicPath: string,
  serverPort: number,
  clientDevServerPort: number,
};
